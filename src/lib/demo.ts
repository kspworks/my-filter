/**
 * The showcase account `pnpm db:seed` creates.
 *
 * It may exist in production so the UI can be demonstrated, with cartridges
 * that are overdue and due soon on purpose — so it is never a real reader, and
 * the daily digest skips it.
 */
export const DEMO_EMAIL = "demo@myfilter.app";
